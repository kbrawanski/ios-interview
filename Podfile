# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'chargingstationtesting' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for chargingstationtesting

  target 'chargingstationtestingTests' do
    inherit! :search_paths
    pod 'ViewInspector'
    pod 'Quick'
    pod 'Nimble'
    pod 'Cuckoo'
    # Pods for testing
  end

  target 'chargingstationtestingUITests' do
    # Pods for testing
  end

end
